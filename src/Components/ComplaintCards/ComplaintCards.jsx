import React, { use } from 'react';

const ComplaintCards = ({complaints}) => {
    const complaintDatas = use(complaints)
    console.log(complaintDatas);
    return (
        <div>
            
        </div>
    );
};

export default ComplaintCards;